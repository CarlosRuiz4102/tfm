\chapter{Anexos}

\section{Ejecuci\'on del sistema}

Este anexo recoge la informaci\'on operativa necesaria para ejecutar y defender el proyecto. El punto de entrada principal es \texttt{run\_mvp.py}, que recibe una consulta en lenguaje natural, construye el workflow multiagente y devuelve un JSON final con el estado completo de la ejecuci\'on.

\subsection{Preparaci\'on m\'inima}

Antes de lanzar una ejecuci\'on conviene comprobar cuatro condiciones:

\begin{enumerate}
    \item que el entorno Python tenga instaladas las dependencias del proyecto;
    \item que el fichero \texttt{.env} contenga una configuraci\'on LLM v\'alida;
    \item que el entorno tenga conectividad suficiente para acceder tanto al endpoint LLM como a \texttt{yfinance};
    \item que el proceso pueda escribir los artefactos generados en la carpeta \path{results}.
\end{enumerate}

Una instalaci\'on m\'inima puede hacerse con:

\begin{verbatim}
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
\end{verbatim}

\subsection{Ejecuci\'on con ejemplos integrados}

El proyecto incluye varios casos ya preparados en \path{src/examples/sample_inputs.py}. Estos ejemplos sirven para lanzar consultas representativas sin tener que redactarlas manualmente y permiten comprobar el flujo de extremo a extremo con peticiones ya definidas dentro del repositorio. La forma m\'as simple de probar el sistema es ejecutar uno de esos ejemplos:

\begin{verbatim}
python run_mvp.py --example growth_nvda
python run_mvp.py --example returns_qqq_spy
python run_mvp.py --example complex_nvda_growth_report
\end{verbatim}

Esta v\'ia es la m\'as \'util para una primera validaci\'on, porque evita errores de formato en la entrada y permite observar c\'omo el sistema resuelve la petici\'on, descarga los datos necesarios y genera la respuesta final.

\subsection{Ejecuci\'on con una entrada JSON propia}

Tambi\'en es posible lanzar el sistema con un fichero JSON preparado manualmente:

\begin{verbatim}
python run_mvp.py --input-json ruta\al\caso.json
\end{verbatim}

La estructura m\'inima esperada es la siguiente:

\begin{footnotesize}
\begin{verbatim}
{
  "query": "Analiza los retornos de QQQ y SPY en 2024"
}
\end{verbatim}
\end{footnotesize}

La entrada m\'inima solo necesita el campo \texttt{query}. A partir de esa petici\'on, el workflow resuelve los instrumentos, el rango temporal y los par\'ametros de descarga antes de pasar a la fase anal\'itica. La ejecuci\'on normal del sistema no depende de proporcionar rutas de CSV locales en la entrada.

\subsection{Salida y artefactos generados}

La ejecuci\'on imprime por consola un JSON con el estado final del workflow. Ese estado incluye, entre otros elementos:

\begin{itemize}
    \item la consulta original y su forma normalizada;
    \item el plan anal\'itico generado por el agente planificador;
    \item el c\'odigo Python producido para el c\'alculo;
    \item la salida estructurada de la ejecuci\'on;
    \item la respuesta final redactada para el usuario;
    \item el estado de la ejecuci\'on, los avisos y los posibles errores.
\end{itemize}

Adem\'as, el sistema conserva artefactos intermedios en dos carpetas:

\begin{itemize}
    \item \path{results/data_raw} y \path{results/data_normalized}, con los artefactos generados durante la fase de descarga y normalizaci\'on de datos;
    \item \path{results/code}, con el c\'odigo generado en cada ejecuci\'on;
    \item \path{results/logs}, con el payload enviado al ejecutor y los ficheros de \texttt{stdout} y \texttt{stderr}.
\end{itemize}

Esta conservaci\'on resulta \'util tanto para depurar fallos como para defender metodol\'ogicamente la trazabilidad del flujo en la memoria.

\subsection{Recorrido de un caso no completado}
\label{anx:caso_no_completado}

Como complemento al caso completado presentado en el Cap\'itulo~5, este apartado resume una traza real que no lleg\'o a completarse, pero que fall\'o
deliberadamente tarde en el flujo. La consulta elegida fue: \emph{``Compara MSFT, AAPL y GOOGL en el \'ultimo a\~no como informe para un cliente no
t\'ecnico. Muestra exactamente cuatro bloques: resumen ejecutivo, tabla de m\'etricas, ranking mensual y limitaciones.''}

La ventaja de este ejemplo es que no se bloquea en la fase de datos, sino que atraviesa la resoluci\'on de instrumentos, la descarga real, la planificaci\'on
anal\'itica, la generaci\'on de c\'odigo y la validaci\'on est\'atica. El fallo solo aparece en la ejecuci\'on real del script y, por tanto, deja una evidencia
m\'as rica de c\'omo act\'uan las rutas de reparaci\'on tard\'ias del workflow.

\subsubsection{Fase 1. Resoluci\'on y validaci\'on de datos}

En este caso, el Agente 1 construye sin ambig\"uedad un \texttt{FinancialDataRequest} con tres tickers, periodo anual e intervalo diario:

\begin{lstlisting}[style=traceblock]
{
  "user_query": "Compara MSFT, AAPL y GOOGL en el último año como informe para un cliente no técnico. Muestra exactamente cuatro bloques: resumen ejecutivo, tabla de métricas, ranking mensual y limitaciones",
  "provider": "yfinance",
  "instruments": [
    {"ticker": "MSFT"},
    {"ticker": "AAPL"},
    {"ticker": "GOOGL"}
  ],
  "interval": "1d",
  "start": null,
  "end": null,
  "period": "1y",
  "required_fields": [
    "Open",
    "High",
    "Low",
    "Close",
    "Adj Close",
    "Volume"
  ],
  "needs_clarification": false,
  "clarification_reason": null
}
\end{lstlisting}

La validaci\'on estructural no detecta problemas y la descarga se completa correctamente. El handoff que recibe el Agente 2 ya contiene una base consolidada
de 756 filas, con los tres tickers resueltos y el CSV normalizado listo para reutilizarse:

\begin{lstlisting}[style=traceblock]
{
  "query": "Compara MSFT, AAPL y GOOGL en el último año como informe para un cliente no técnico. Muestra exactamente cuatro bloques: resumen ejecutivo, tabla de métricas, ranking mensual y limitaciones",
  "tickers": [
    "MSFT",
    "AAPL",
    "GOOGL"
  ],
  "temporal_context": {
    "start": null,
    "end": null,
    "period": "1y",
    "interval": "1d"
  },
  "csv_paths": [
    "C:\\Users\\usuario\\Desktop\\tfm\\results\\data_normalized\\normalized_20260617_183459_729914.csv"
  ],
  "data_context": {
    "row_count": 756,
    "available_columns": [
      "Date",
      "Ticker",
      "Open",
      "High",
      "Low",
      "Close",
      "Adj Close",
      "Volume"
    ]
  },
  "warnings": [],
  "download_summary": {
    "provider": "yfinance",
    "tickers_requested": [
      "MSFT",
      "AAPL",
      "GOOGL"
    ],
    "tickers_found": [
      "AAPL",
      "GOOGL",
      "MSFT"
    ],
    "interval": "1d",
    "period": "1y",
    "start": null,
    "end": null,
    "row_count": 756
  }
}
\end{lstlisting}

\subsubsection{Fase 2. Planificaci\'on anal\'itica y generaci\'on de c\'odigo}

Con ese contexto operativo resuelto, el Agente 2 formula un plan comparativo orientado a rentabilidad, volatilidad, drawdown, ratio Sharpe y ranking
mensual:

\begin{lstlisting}[style=traceblock]
{
  "analytical_goal": "Comparar el desempeño de MSFT, AAPL y GOOGL durante el último año y presentar los resultados en un informe accesible para un cliente no técnico, con cuatro bloques: resumen ejecutivo, tabla de métricas, ranking mensual y limitaciones.",
  "analysis_type": "comparative_return_analysis",
  "metrics": [
    "Annual Return",
    "Annual Volatility",
    "Maximum Drawdown",
    "Sharpe Ratio"
  ],
  "required_columns": [
    "Date",
    "Ticker",
    "Adj Close"
  ],
  "output_requirements": [
    "metrics: un objeto JSON con los valores calculados para cada ticker.",
    "summary: un texto plano de máximo 200 palabras que sintetice los hallazgos clave.",
    "limitations: una lista de al menos tres limitaciones metodológicas o de datos.",
    "table_metrics: una tabla (JSON array de objetos) con las métricas por ticker.",
    "ranking_monthly: una lista ordenada de los tickers por rendimiento mensual promedio."
  ],
  "reasoning": "El usuario solicita una comparación de tres tickers con cuatro bloques estructurados. Se selecciona el análisis de retorno comparativo porque cubre rendimiento, volatilidad y drawdown, y permite generar un ranking mensual."
}
\end{lstlisting}

El Agente 3 transforma ese plan en un script ejecutable. Durante la validaci\'on previa hubo una reparaci\'on de c\'odigo, pero el Agente 4 termin\'o aceptando
la versi\'on final como v\'alida para ejecutarse. Uno de los fragmentos relevantes del script generado es este:

\begin{lstlisting}[style=traceblock]
monthly_ret = close.resample('ME').last().pct_change().dropna()
avg_monthly = monthly_ret.mean()
ranking = sorted(avg_monthly.items(), key=lambda x: x[1], reverse=True)
output['ranking_monthly'] = [ticker for ticker, _ in ranking]

summary_text = (
    f"En el último año, Microsoft (MSFT) mostró el mayor rendimiento anual con un retorno del {metrics.get('MSFT', {{}}).get('Annual Return', 0):.2%}, "
    f"seguido de Apple (AAPL) con {metrics.get('AAPL', {{}}).get('Annual Return', 0):.2%} y Google (GOOGL) con {metrics.get('GOOGL', {{}}).get('Annual Return', 0):.2%}. "
)
\end{lstlisting}

La validaci\'on est\'atica, de hecho, no bloquea la ejecuci\'on:

\begin{lstlisting}[style=traceblock]
{
  "decision": "valid",
  "errors": [],
  "warnings": [],
  "required_fixes": [],
  "reasoning": "El script implementa correctamente el plan analítico solicitado y contiene los bloques de salida esperados."
}
\end{lstlisting}

\subsubsection{Fase 3. Ejecuci\'on real, reparaci\'on y cierre con error}

La parte interesante de esta traza empieza precisamente aqu\'i. Aunque el script hab\'ia superado la validaci\'on est\'atica, la ejecuci\'on real falla en runtime
al construir el resumen final:

\begin{lstlisting}[style=traceblock]
Traceback (most recent call last):
  File "C:\Users\usuario\Desktop\tfm\results\code\generated_20260617_183939_690053.py", line 53, in <module>
    f"En el último año, Microsoft (MSFT) mostró el mayor rendimiento anual con un retorno del {metrics.get('MSFT', {{}}).get('Annual Return', 0):.2%}, "
                                                                                                                   ^^^^
TypeError: unhashable type: 'dict'
\end{lstlisting}

Ese fallo no se trata como bloqueo inmediato, sino como una salida \texttt{repairable}. El validador de ejecuci\'on deja constancia expl\'icita de ello:

\begin{lstlisting}[style=traceblock]
{
  "execution_validation_decision": {
    "decision": "repairable",
    "errors": [
      "La ejecución terminó con returncode=1 y un TypeError al construir el resumen final."
    ],
    "warnings": [],
    "reasoning": "El script falló durante la ejecución real y necesita una corrección antes de reintentarse."
  }
}
\end{lstlisting}

El workflow intenta entonces una reparaci\'on tard\'ia de runtime. Sin embargo, esa reparaci\'on tampoco consigue cerrar el caso y el flujo termina con error
controlado. El snapshot terminal deja una evidencia muy clara del cierre:

\begin{lstlisting}[style=traceblock]
{
  "final_answer": "No se pudo reparar el código tras un error de ejecución: Expecting value: line 1 column 1 (char 0)",
  "warnings": [
    "Se reparó código generado tras un error previo.",
    "Se reparó código tras un error de ejecución."
  ],
  "error_message": "No se pudo reparar el código tras un error de ejecución: Expecting value: line 1 column 1 (char 0)",
  "status": "completed_with_error",
  "code_repair_attempts": 1,
  "execution_attempts": 2,
  "execution_repair_attempts": 1
}
\end{lstlisting}

\subsubsection{Lectura del caso}

Este ejemplo resulta \'util porque muestra una situaci\'on menos trivial que un bloqueo temprano. El sistema s\'i resuelve los datos, s\'i construye el plan y s\'i
llega a ejecutar c\'odigo real sobre un CSV descargado. El problema aparece solo cuando el c\'odigo ya est\'a siendo usado de verdad, lo que permite observar
mejor la diferencia entre validaci\'on est\'atica y validaci\'on en runtime.

Tambi\'en deja ver que una ejecuci\'on no completada no equivale necesariamente a una arquitectura opaca. Aunque el caso termina mal, la traza conserva el
\texttt{FinancialDataRequest}, los artefactos de descarga, el plan anal\'itico, el script ejecutado, el \texttt{stderr} real, el intento de reparaci\'on y el
estado terminal. Precisamente por eso este tipo de ejemplo complementa bien el recorrido del Cap\'itulo~5: ense\~na c\'omo falla el sistema cuando el error aparece
lo m\'as tarde posible, pero sin perder trazabilidad ni evidencia observable.

\section{Configuraci\'on LLM y API}

La aplicaci\'on utiliza un endpoint compatible con OpenAI Chat Completions. El modelo \texttt{openai/gpt-oss-20b} \cite{openai2025gptossmodelcard} se sirve a trav\'es de vLLM, aunque el c\'odigo acepta varias familias de variables de entorno equivalentes.

\subsection{Configuraci\'on recomendada}

La forma m\'as directa de configurar el sistema es declarar en \texttt{.env}:

\begin{verbatim}
LLM_PROVIDER=openai-compatible
VLLM_BASE_URL=https://servidor-vllm.example/v1
VLLM_API_KEY=pegar_aqui_la_clave
VLLM_MODEL=openai/gpt-oss-20b
\end{verbatim}

\subsection{Variables equivalentes admitidas}

El c\'odigo reconoce tres familias de nombres para base URL, clave y modelo:

\begin{itemize}
    \item familia \texttt{VLLM\_*}: \texttt{VLLM\_BASE\_URL}, \texttt{VLLM\_API\_KEY}, \texttt{VLLM\_MODEL};
    \item familia \texttt{LLM\_*}: \texttt{LLM\_BASE\_URL}, \texttt{LLM\_API\_KEY}, \texttt{LLM\_MODEL};
    \item familia \texttt{OPENAI\_*}: \texttt{OPENAI\_BASE\_URL}, \texttt{OPENAI\_API\_KEY}, \texttt{OPENAI\_MODEL}.
\end{itemize}

La resoluci\'on de configuraci\'on prioriza primero los valores \texttt{VLLM\_*}, despu\'es los valores gen\'ericos \texttt{LLM\_*} y finalmente los alias \texttt{OPENAI\_*}.

\subsection{Par\'ametros de generaci\'on}

Tambi\'en pueden ajustarse los par\'ametros principales del cliente:

\begin{verbatim}
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=4096
LLM_TIMEOUT_SECONDS=120
OPENAI_VERIFY_SSL=true
\end{verbatim}

Estos valores controlan la temperatura de generaci\'on, el m\'aximo de tokens de salida, el tiempo de espera del cliente y la verificaci\'on SSL del endpoint remoto.

\subsection{Comprobaciones pr\'acticas}

Si la configuraci\'on es correcta, una ejecuci\'on con \texttt{run\_mvp.py} debe completar el flujo resolviendo la consulta, descargando los datos hist\'oricos necesarios y generando despu\'es una respuesta trazable. Si la clave no est\'a definida o contiene un valor de ejemplo, el sistema no intenta llamar al modelo y devuelve un error de configuraci\'on expl\'icito.

\section{Notas para reproducibilidad}

Este anexo sirve como referencia corta de reproducci\'on operativa. Para repetir un caso hacen falta el mismo c\'odigo, una configuraci\'on equivalente del modelo, acceso a un endpoint compatible con OpenAI y disponibilidad de los datos hist\'oricos correspondientes en \texttt{yfinance}. Si alguno de estos elementos cambia, el comportamiento del sistema puede variar aunque la arquitectura general siga siendo la misma.
