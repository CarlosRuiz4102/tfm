\chapter{Implementación}

\section{Estructura del código}

La implementación se organiza en módulos pequeños:

\begin{table}[H]
\centering
\footnotesize
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{4.5cm}X}
\toprule
\textbf{Módulo} & \textbf{Responsabilidad} \\
\midrule
\texttt{schemas.py} & Define la entrada, el plan interpretado, los artefactos y el estado del workflow. \\
\texttt{validation.py} & Valida query, tickers, fechas y rutas CSV antes de llamar al LLM. \\
\texttt{nodes.py} & Implementa los nodos del flujo: análisis LLM, codegen, seguridad, ejecución e interpretación. \\
\texttt{src/llm/} & Contiene prompts, cliente compatible con OpenAI y funciones del pipeline LLM. \\
\texttt{code\_security.py} & Revisa el código generado antes de ejecutarlo. \\
\texttt{code\_runner.py} & Guarda el script, lanza la ejecución y captura stdout, stderr y artefactos. \\
\texttt{market\_data.py} & Proporciona helpers para leer CSV financieros descargados con \texttt{yfinance}. \\
\texttt{tests/} & Comprueba el workflow con llamadas LLM simuladas y escenarios de error. \\
\bottomrule
\end{tabularx}
\caption{Módulos principales de la implementación.}
\end{table}

\section{Repositorio y organización del trabajo}

El desarrollo se ha gestionado en un repositorio GitHub disponible en \nolinkurl{https://github.com/CarlosRuiz4102/tfm}. El uso de control de versiones permite conservar la evolución del código, la memoria, los scripts de evaluación y la documentación auxiliar en un único espacio de trabajo. Esta organización facilita revisar qué parte corresponde a datos, qué parte corresponde al flujo LLM, qué parte corresponde a resultados y qué parte corresponde a documentación académica.

La estructura del repositorio separa responsabilidades de forma cercana a una práctica MLOps ligera:

\begin{itemize}
    \item \texttt{data/}: contiene el catálogo de casos, los CSV históricos congelados y sus metadatos.
    \item \texttt{src/}: agrupa el código de aplicación, dividido en esquemas, grafo, integración LLM y ejecución controlada.
    \item \texttt{scripts/}: incluye utilidades para generar EDA, ejecutar evaluaciones y producir informes reproducibles.
    \item \texttt{tests/}: valida el workflow con respuestas LLM simuladas, evitando depender de APIs externas durante las pruebas básicas.
    \item \texttt{docs/}: conserva la memoria LaTeX, informes Markdown, figuras y registros de resultados.
    \item \texttt{results/}: almacena artefactos generados durante ejecuciones, como scripts, payloads, logs y salidas JSON.
\end{itemize}

También se separan configuración y secretos. El fichero \texttt{.env} contiene claves locales y queda excluido del control de versiones, mientras que \texttt{.env.example} documenta las variables necesarias para reproducir la configuración sin exponer credenciales. Del mismo modo, \texttt{.gitignore} excluye entornos virtuales, cachés, logs y resultados generados de gran tamaño, manteniendo versionados los artefactos que aportan trazabilidad académica.

Aunque el proyecto no implementa un ciclo MLOps productivo completo con despliegue continuo, sí incorpora prácticas propias de ese enfoque: versionado del código, separación entre datos, código y resultados, scripts de evaluación repetibles, pruebas automatizadas, conservación de artefactos, documentación de configuraciones y registro explícito de fallos. Para el alcance del TFM, estas prácticas son suficientes para defender que el sistema no se limita a una demo puntual, sino que se ha organizado como un prototipo reproducible y auditable.

\section{Contratos de datos}

La entrada del sistema se representa mediante \texttt{FinancialQueryInput}. La interpretación generada por el LLM se almacena en \texttt{AnalysisPlan}, que contiene intencionalidad interpretada, tipo de análisis, métricas esperadas, requisitos de datos, requisitos de salida, preferencias de presentación y razonamiento resumido. El estado completo se conserva en \texttt{WorkflowState}.

La necesidad de helpers de datos apareció durante las pruebas documentadas en \nolinkurl{docs/plan_pruebas_queries_llm.md}: los CSV generados por \texttt{yfinance} pueden contener cabeceras multinivel y formatos distintos según el activo o el intervalo. Para reducir errores repetidos en el código generado, se incorporaron funciones reutilizables de carga y normalización. Así, el LLM no tiene que resolver desde cero cada variante de lectura de CSV y puede concentrarse en el cálculo solicitado.

\section{Pipeline LLM}

El pipeline LLM se divide en tres llamadas principales. La primera interpreta la consulta y construye un plan estructurado. La segunda genera código Python usando tanto el plan como el mensaje original del usuario. La tercera interpreta la salida JSON producida por el script y redacta la respuesta final.

El prompt de generación de código exige que el script lea un payload por \texttt{argv[1]}, use librerías permitidas y escriba un único JSON en \texttt{stdout}. Esta restricción permite tratar el código generado como un artefacto ejecutable, auditable y validable.

El flujo completo puede resumirse como una cadena de transformación controlada:

\begin{quote}
\centering
\texttt{prompt} \(\rightarrow\) \texttt{código Python} \(\rightarrow\) validación de seguridad \(\rightarrow\) ejecución \(\rightarrow\) \texttt{JSON} \(\rightarrow\) interpretación final.
\end{quote}

Esta cadena es importante porque el LLM no produce directamente una afirmación financiera final sin pasar antes por cálculos ejecutados. El primer bloque LLM prepara el análisis y genera el script; el entorno de ejecución produce datos estructurados; y el segundo bloque LLM redacta una explicación basada en esa salida.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{3.5cm}X}
\toprule
\textbf{Elemento} & \textbf{Contrato exigido} \\
\midrule
Entrada & El script recibe un único payload JSON por \texttt{argv[1]}, con consulta, tickers, fechas, intervalo y rutas CSV. \\
Lectura de datos & El código debe leer únicamente los CSV proporcionados por el workflow y no descargar datos externos. \\
Salida & Debe escribir un único objeto JSON en \texttt{stdout}, mediante \texttt{json.dumps}, para que el sistema pueda parsearlo. \\
Contenido & La salida debe incluir métricas, tablas o datos de visualización necesarios para responder a la consulta. \\
Artefactos & La ejecución conserva script, salida estándar, salida de error, código de retorno y metadatos de ejecución. \\
Errores & Si el script falla, el error queda registrado y el workflow puede devolver una explicación controlada o intentar reparación. \\
\bottomrule
\end{tabularx}
\caption{Contrato operativo del código Python generado por el LLM.}
\label{tab:contrato_codigo_generado}
\end{table}

\section{Control de seguridad}

Antes de ejecutar el script, el módulo \texttt{code\_security.py} analiza el código con el AST de Python. El validador comprueba imports permitidos, llamadas bloqueadas como \texttt{eval} o \texttt{exec}, presencia de \texttt{main()} y uso de \texttt{json.dumps}. Si el código no cumple el contrato, el workflow termina con estado de error y no ejecuta el script.

El uso del árbol de sintaxis abstracta permite revisar la estructura del programa antes de ejecutarlo, en lugar de depender solo de búsquedas textuales. La validación comprueba nodos de importación, llamadas a funciones y elementos mínimos del contrato. Con ello se reduce el riesgo de que un script generado por LLM ejecute operaciones fuera del alcance del MVP. Esta capa no convierte el entorno en una sandbox completa, pero sí añade una barrera explícita entre generación y ejecución.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{Comprobación} & \textbf{Finalidad} \\
\midrule
Imports permitidos & Limitar el código a librerías necesarias para análisis de datos y serialización. \\
Funciones bloqueadas & Evitar llamadas peligrosas o no necesarias, como \texttt{eval} y \texttt{exec}. \\
Función \texttt{main()} & Forzar una estructura ejecutable y sencilla de auditar. \\
Salida JSON & Garantizar que el resultado pueda pasar al nodo de interpretación sin ambigüedad. \\
Errores explícitos & Separar fallos de seguridad, fallos de ejecución y fallos de interpretación. \\
\bottomrule
\end{tabularx}
\caption{Comprobaciones principales del validador de seguridad.}
\label{tab:validador_seguridad_ast}
\end{table}

\section{Reparación y compactación}

Cuando el código generado no supera la validación o falla durante la ejecución, el workflow puede solicitar al LLM una reparación manteniendo la consulta original, el plan analítico y el detalle del error. La reparación no evita la validación: el código corregido vuelve a pasar por el mismo control de seguridad antes de ejecutarse. Si la reparación tampoco cumple el contrato, el caso se marca como error controlado.

En consultas de Nivel B o C, el script puede generar series visuales largas. Estas series son útiles como artefacto completo, pero pueden saturar el contexto del segundo agente. Por ello, antes de la interpretación final se compactan listas y textos extensos: se conserva una muestra representativa para el LLM interpretador y la salida completa queda guardada como evidencia auditable. Esta decisión permite mantener trazabilidad sin degradar la respuesta final por exceso de contexto.

Este ajuste quedó documentado en una nota interna de control de tamaño de salidas, referenciada en el Anexo~\ref{tab:documentacion_markdown_memoria}. La decisión práctica fue limitar o resumir datos de visualización enviados al segundo agente, manteniendo siempre la salida completa en los logs. De este modo, las consultas de Nivel B y C pueden producir material gráfico sin convertir la interpretación final en una llamada demasiado grande para el proveedor LLM.

\section{Pruebas}

La batería de pruebas simula las respuestas LLM para evitar dependencias externas durante el test suite. De este modo se valida el workflow, el control de seguridad, la ejecución controlada y los errores de entrada sin consumir APIs reales.



