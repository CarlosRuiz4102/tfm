DUDAS:
-LLM o parser + alias locales pequeños + resolución dinámica + caché

Cosas secundarias: 
- Realizar la primera etapa
- Detectar sentimiento en noticias financieras positivas/neutras/negativas con FinBERT
- Intentemos realizar una prediccion a futuro? muy dificil??

--------------------------------------------------------------------------------
TAREAS:

-mirar el requirements para instalarlo como lo hace pablo, desde colab
-empezar a realizar la memoria

consensus buscar papers

-------------------------------------------------------------------------------

PRINCIPAL: Diseño e implementación de un sistema multiagéntico analista capaz de interpretar peticiones de usuarios 
sobre datos financieros y generar automáticamente un análisis útil a partir de ellas.

La idea principal parte de una query en lenguaje natural introducida por el usuario, por ejemplo: “Cuánto ha crecido
Nvidia en 5 años” o “Compara Nvidia y AMD en 2 años”. Esa consulta se transforma previamente en una estructura más 
formal con campos definidos, como la intención de la petición, los tickers involucrados, las fechas de inicio y fin o, 
en su defecto, el periodo y el intervalo temporal de los datos. De esta manera, cada consulta queda representada de forma 
normalizada y preparada para ser procesada por el sistema.

A partir de esa query estructurada, el sistema obtiene también los datos históricos en formato CSV desde la librería 
financiera correspondiente, de forma que dispone tanto de la intención del usuario como de los datos necesarios para responder.
Con estos dos elementos, la arquitectura propuesta introduce un agente orquestador, cuya función es identificar qué tipo de 
tarea debe realizarse y derivarla al agente especializado adecuado dentro del sistema multiagente.

El agente orquestador constituye la pieza central de coordinación del sistema. Su responsabilidad no consiste en ejecutar 
directamente el análisis financiero, sino en interpretar la query estructurada, validar sus parámetros y decidir qué agente 
especializado debe intervenir. Para ello, comprueba que la petición sea coherente, que los tickers estén correctamente 
definidos, que el número de activos coincida con el tipo de análisis solicitado y que el rango temporal o el intervalo sean 
compatibles con los datos requeridos. Una vez validada la entrada, el orquestador enruta la consulta hacia el agente que mejor 
se ajusta a la intención detectada.

Con el fin de mantener un alcance claro y coherente con los objetivos del TFM, se definen seis intenciones analíticas 
principales, todas ellas orientadas al análisis histórico y descriptivo de activos financieros.
| Intención            		| Objetivo                                                   | Entrada mínima                   | Salida esperada                                           |
| --------------------------| ---------------------------------------------------------- | -------------------------------- | --------------------------------------------------------- |
| `asset_overview`     		| Ofrecer una visión general del comportamiento de un activo | 1 ticker + rango temporal        | resumen descriptivo, métricas básicas y gráfico           |
| `price_growth`       		| Calcular crecimiento o decrecimiento entre dos fechas      | 1 ticker + rango temporal        | variación absoluta, porcentual e interpretación           |
| `compare_assets`     		| Comparar el comportamiento de varios activos               | 2 o más tickers + rango temporal | comparación normalizada, tabla de métricas y conclusiones |
| `return_analysis`    		| Estudiar la serie de retornos del activo                   | 1 o más tickers + rango temporal | retornos, distribución, medias y extremos                 |
| `historical_risk_analysis`| Evaluar el riesgo histórico de uno o varios activos        | 1 o más tickers + rango temporal | volatilidad, drawdown, ratios simples y análisis          |
| `technical_analysis` 		| Analizar indicadores técnicos sobre precios históricos     | 1 ticker mínimo + rango temporal | indicadores, señales y gráficos técnicos                  |
Todas estas intenciones se limitan al análisis histórico de datos financieros. En consecuencia, el sistema no incorpora módulos 
de predicción futura, forecasting ni estimación prospectiva, sino que centra su funcionamiento en la interpretación descriptiva 
y comparativa de series temporales históricas.

Una característica importante de la propuesta es que la salida de estos agentes no es solo un dato numérico o un texto breve, 
sino un código en Python generado específicamente para resolver la tarea solicitada y mostrar el resultado. Ese código se 
ejecuta posteriormente para producir la salida final, que incluye tanto el análisis cuantitativo como sus interpretaciones. 
Por tanto, el sistema no solo comprende la consulta y selecciona el agente adecuado, sino que además automatiza la construcción
de la respuesta analítica final.

En otras palabras, el TFM plantea una tubería completa en la que:
1.el usuario formula una consulta financiera en lenguaje natural,
2.esa consulta se transforma en una estructura interpretable por el sistema,
3.se recuperan los datos históricos asociados,
4.un orquestador decide qué agente especializado debe intervenir,
5.el agente genera el código necesario para analizar los datos,
6.dicho código se ejecuta,
7.y finalmente se obtiene un resultado analítico comprensible para el usuario.

Este flujo coincide con el esquema que aparece en tu diagrama: 
Query + Datos CSV → Orquestador → Multiagente Analista → Respuesta → Ejecución → Análisis

--------------------------------------------------------------------------------

Objetivo práctico:
-Mantener la parte de query + CSV ya resuelta y centrar el desarrollo nuevo en: orquestación, generación analítica y 
ejecución segura.

Librerías eje:
- LangGraph: para definir el grafo del sistema multiagente (estado compartido, nodos, transiciones, router y control 
del flujo).
- vLLM: para servir el LLM localmente o en servidor con API compatible con OpenAI.
- subprocess: para ejecutar el código Python generado en un proceso aislado y capturar stdout, stderr y código de salida.

Decisión de arquitectura:
-No empezar con muchos agentes autónomos.
-Empezar con un workflow controlado en LangGraph con pocos nodos y luego especializar.
-Primera versión recomendada:
  1. nodo_router_orchestrator
  2. nodo_analysis_agent
  3. nodo_code_generator
  4. nodo_code_executor
  5. nodo_result_interpreter

Evolución posterior:
-Dividir el nodo_analysis_agent en agentes por intención:
  - asset_overview_agent
  - price_growth_agent
  - compare_assets_agent
  - return_analysis_agent
  - historical_risk_analysis_agent
  - technical_analysis_agent

Por qué LangGraph encaja:
-Permite modelar el sistema como grafo con estado compartido.
-Cada nodo puede ser LLM o código Python normal.
-Permite routing condicional.
-Permite separar claramente workflow fijo vs comportamiento agentic.
-Facilita debugging y trazabilidad del flujo.

Estado compartido mínimo del grafo:
- user_query: str
- normalized_query: dict
- csv_paths: list[str]
- detected_intent: str
- selected_agent: str
- analysis_plan: dict
- generated_code: str
- execution_stdout: str
- execution_stderr: str
- execution_returncode: int
- final_answer: str
- warnings: list[str]

Nodos mínimos recomendados en LangGraph:

1. ingest_node
-Recibe query estructurada y rutas a CSV.
-Verifica que existe la entrada mínima.
-No usa LLM.

2. orchestrator_router_node
-Valida intención, número de tickers, rango temporal e intervalo.
-Decide si la tarea es válida.
-Selecciona agente especialista.
-Si hay error, enruta a invalid_request_node.

3. specialist_analysis_node
-Recibe intención + metadatos + CSV.
-Genera un plan analítico concreto.
-No debería devolver texto libre ambiguo, sino una estructura clara del análisis a realizar.

4. code_generation_node
-Genera código Python ejecutable a partir del plan.
-Debe trabajar con prompt muy restringido.
-Debe devolver solo código o JSON con campo code.

5. code_execution_node
-Ejecuta el script con subprocess.
-Captura stdout, stderr y returncode.
-Puede escribir resultados en carpeta results/.

6. interpretation_node
-Toma métricas/salidas del script y redacta la respuesta final en lenguaje natural.
-También puede resumir warnings o limitaciones.

7. invalid_request_node
-Maneja problemas de validación.

8. execution_error_node
-Maneja errores del script generado.

Orden real recomendado del pipeline:
-query estructurada + CSV -> ingest -> router/orchestrator -> specialist_analysis -> code_generation -> code_execution -> interpretation -> output final

Muy importante sobre el primer agente:
-El primer agente no debe producir directamente una respuesta final, sino un objeto de trabajo normalizado y validado.
-Si la etapa de query ya está hecha, LangGraph debería arrancar justo desde esa entrada estructurada y no rehacerla.

Interfaz entre vuestro parser actual y LangGraph:
-Entrada al grafo:
  {
    "query": "Compara Nvidia y AMD en 2 años",
    "intent": "compare_assets",
    "tickers": ["NVDA", "AMD"],
    "start": null,
    "end": null,
    "period": "2y",
    "interval": "1d",
    "csv_paths": ["data/raw/NVDA_2y_1d.csv", "data/raw/AMD_2y_1d.csv"]
  }

Rol exacto de vLLM en el sistema:
-vLLM no organiza el pipeline; solo sirve el modelo.
-LangGraph orquesta.
-vLLM responde a las llamadas del LLM.
-El modelo se invoca desde LangChain/LangGraph como si fuera OpenAI, cambiando base_url y api_key.

Configuración conceptual esperada:
-Servidor vLLM levantado con el modelo elegido.
-Cliente OpenAI o ChatOpenAI apuntando a http://localhost:8000/v1.
-LangGraph usa ese cliente en los nodos LLM.

Qué tareas sí conviene delegar al LLM:
-elegir o confirmar intención si alguna vez llega ambigua
-generar plan analítico
-generar código Python
-interpretar resultado final

Qué tareas NO conviene delegar al LLM si ya están resueltas por código:
-parsing principal de la query
-resolución de ticker si ya existe
-normalización temporal si ya existe
-validación determinista de intervalo/periodo
-carga de CSV

Idea importante para robustez:
-Usar salidas estructuradas en vez de texto libre cuando el nodo tenga que devolver:
  - intención
  - plan analítico
  - nombre del agente
  - bloque de código

Primer MVP realista:
-MVP 1:
  - usar solo 2 intenciones: price_growth y compare_assets
  - un único specialist_analysis_node que cambie prompt según intent
  - code_generation_node con plantilla muy cerrada
  - code_execution_node con timeout
  - interpretation_node simple

-MVP 2:
  - añadir asset_overview y return_analysis
  - separar specialist nodes por familias

-MVP 3:
  - añadir historical_risk_analysis y technical_analysis
  - mejorar prompts y evaluación

Propuesta de estructura de proyecto actualizada:

tfm-financial-multiagent/
│
├── README.md
├── requirements.txt
├── .env.example
├── .gitignore
│
├── data/
│   ├── raw/
│   ├── processed/
│   └── generated/
│
├── notebooks/
│   ├── 01_yfinance_eda_avanzado.ipynb
│   ├── 02_catalogo_queries_y_csv.ipynb
│   └── 03_langgraph_mvp.ipynb
│
├── src/
│   ├── config.py
│   ├── schemas.py
│   ├── prompts.py
│   ├── graph/
│   │   ├── state.py
│   │   ├── nodes.py
│   │   ├── routing.py
│   │   └── build_graph.py
│   ├── agents/
│   │   ├── overview_agent.py
│   │   ├── growth_agent.py
│   │   ├── compare_agent.py
│   │   ├── returns_agent.py
│   │   ├── risk_agent.py
│   │   └── technical_agent.py
│   ├── execution/
│   │   ├── code_runner.py
│   │   └── sandbox_utils.py
│   ├── io/
│   │   ├── csv_loader.py
│   │   └── result_writer.py
│   └── evaluation/
│       ├── benchmark_intents.py
│       ├── benchmark_code_exec.py
│       └── benchmark_outputs.py
│
├── results/
│   ├── figures/
│   ├── tables/
│   ├── code/
│   └── logs/
│
└── tests/
    ├── test_routing.py
    ├── test_execution.py
    └── test_prompts.py

Requirements mínimos orientativos:
-langgraph
-langchain
-langchain-openai
-openai
-vllm
-pydantic
-pandas
-numpy
-matplotlib
-yfinance
-python-dotenv

Recomendación sobre instalación:
-Si vais a usar vLLM de verdad, mejor entorno Linux con GPU y entorno limpio.
-No mezclar desde el principio demasiadas dependencias de notebooks y serving.
-Hacer un requirements mínimo para MVP y otro opcional para experimentación.

requirements_mvp.txt
-langgraph
-langchain
-langchain-openai
-openai
-pydantic
-pandas
-numpy
-matplotlib
-python-dotenv

requirements_full.txt
--r requirements_mvp.txt
-vllm
-yfinance
-jupyter
-ipykernel

Subprocess: reglas prácticas
-Ejecutar siempre con lista de argumentos, no con shell=True.
-Usar timeout.
-Capturar stdout/stderr.
-Guardar script generado en archivo temporal o en results/code/.
-Limitar imports permitidos en el prompt del generador.
-Registrar returncode y errores.

Plantilla conceptual de ejecución:
-subprocess.run(["python", script_path], capture_output=True, text=True, timeout=60, check=False)

Qué medir en la evaluación del TFM:
-accuracy de routing por intención
-tasa de ejecución correcta del código generado
-porcentaje de respuestas útiles sin intervención manual
-calidad del análisis final
-tiempo medio por consulta

Riesgos técnicos a vigilar:
-vLLM puede complicar la instalación más que LangGraph.
-La generación libre de código puede romper por pequeños detalles de rutas o columnas.
-Debe existir validación fuerte entre CSV esperados y script generado.

Decisión táctica importante:
-Para empezar, conviene desacoplar el problema en 2 capas:
  1. LangGraph funcionando con un modelo accesible y salida estructurada.
  2. Después sustituir el backend LLM por vLLM si todavía no está listo.

Plan de trabajo recomendado para empezar ya:

Semana / Bloque 1:
-Definir schemas Pydantic del estado y de las salidas estructuradas.
-Implementar build_graph.py con un flujo mínimo.
-Implementar routing determinista basado en intent ya dado.

Bloque 2:
-Crear prompts del specialist_analysis_node y code_generation_node.
-Probar solo con 2 queries del catálogo.

Bloque 3:
-Implementar code_runner.py con subprocess.
-Guardar logs de stdout, stderr y script generado.

Bloque 4:
-Evaluar 10-20 queries de vuestro catálogo.
-Medir fallos por tipo.

Primer entregable técnico útil:
-Un notebook o script que reciba una query estructurada ya resuelta + un CSV y devuelva:
  - intent enrutado
  - código generado
  - resultado ejecutado
  - interpretación final

Preguntas abiertas para ir resolviendo:
-¿Necesitamos un agente distinto por intención desde el inicio o basta un análisis especializado por prompt?
-¿El código generado siempre leerá CSV desde disco o a veces recibirá DataFrame ya cargado?
-¿Queremos que la interpretación final la haga el mismo LLM o una plantilla textual programática?
-¿Vamos a guardar cada ejecución con metadata para la memoria y la evaluación?

Fuera de alcance por ahora:
-predicción futura
-sentiment con noticias
-fundamental analysis complejo
-reasoning con herramientas externas no necesarias

[Entrada: query estructurada + csv_paths]
                |
                v
        [1. Ingest / Validación]
                |
                v
     [2. Orquestador / Router]
          /         |         \
         /          |          \
        v           v           v
 [3A. Growth] [3B. Compare] [3C. Technical/...]
        \           |           /
         \          |          /
                v
        [4. Analysis Planner]
                |
                v
        [5. Code Generator]
                |
                v
        [6. Code Executor]
                |
        +-------+-------+
        |               |
        v               v
 [7A. Error Handler] [7B. Result Interpreter]
        |               |
        +-------+-------+
                |
                v
         [8. Final Response]


------------------------------------------------------

stelterlab/Mistral-Small-24B-Instruct-2501-AWQ

-------------------------------------------------------------------------------

PRIMERA ETAPA: El primer agente no debe producir directamente una respuesta final, sino un objeto de consulta financiera normalizado y validado.

Etapa 1. Interpretación:
Convierte lenguaje natural en una consulta financiera estructurada.

Etapa 2. Resolución:
Transforma nombres y alias en símbolos compatibles con Yahoo Finance.

Etapa 3. Normalización temporal:
Convierte expresiones humanas de tiempo en period o start/end.

Etapa 4. Validación:
Comprueba compatibilidad entre ticker, rango e intervalo.

Etapa 5. Construcción de la petición:
Genera los parámetros de yf.download().

Etapa 6. Descarga:
Obtiene el dataframe bruto.

Etapa 7. Postproceso:
Guarda CSV, metadata y posibles advertencias.

-------------------------------------------------------------------------------